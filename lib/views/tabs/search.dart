import 'package:flutter/material.dart';
import '../../widgets/swippable_cards.dart';

class SearchPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SwippableCards();
  }
}